version: '3.9'

volumes:
  postgres_data:
  backend_data:
  

services:
  backend: &backend
    container_name: backend
    build: .
    ports:
      - "8000:8000"
    volumes:
      - backend_data:/app
    command: gunicorn src.wsgi:application --bind 0.0.0.0:8000